import { PollTimeLeftPipe } from './poll-time-left.pipe';

describe('PollTimeLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new PollTimeLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
