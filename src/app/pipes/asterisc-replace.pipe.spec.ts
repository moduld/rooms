import { AsteriscReplacePipe } from './asterisc-replace.pipe';

describe('AsteriscReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new AsteriscReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
