import { IeHeightDirective } from './ie-height.directive';

describe('IeHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new IeHeightDirective();
    expect(directive).toBeTruthy();
  });
});
