import { ScroolEndDirective } from './scrool-end.directive';

describe('ScroolEndDirective', () => {
  it('should create an instance', () => {
    const directive = new ScroolEndDirective();
    expect(directive).toBeTruthy();
  });
});
